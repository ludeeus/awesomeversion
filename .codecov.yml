comment: false
codecov:
  branch: main

coverage:
  precision: 2
  status:
    patch: off
    project:
      default:
        target: 100%

parsers:
  gcov:
    branch_detection:
      conditional: yes
      loop: yes
      method: no
      macro: no

ignore:
  - "tests"